package net.projetoreviver.sgp.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import net.projetoreviver.sgp.models.CuidadorFrequencia;

public interface CuidadorFrequenciaRepository extends JpaRepository<CuidadorFrequencia, Long> {
    
}
